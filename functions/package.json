{
  "name": "highpoint-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for HighPoint HouseKeep",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-functions": "^7.0.0",
    "firebase-admin": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": "20"
  }
}
